[project]
name = "video-scripts"
version = "0.1.0"
description = "Operator scripts for 8mm video processing workflows."
requires-python = ">=3.13,<3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.3,<9",
]

[tool.uv]
package = false
